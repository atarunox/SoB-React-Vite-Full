// src/data/townLocations/miningOperation.js
// Wrapper for the Mining Operation (Blasted Wastes Town).

import data from './BlastedWastesTown/MiningOperation/miningOperation.js';

const miningOperation = {
  id: data.id,
  name: data.name,
  town: data.town,
  type: data.type,
  description: data.description,
  events: data.events,
};

export default miningOperation;
