// src/data/townLocations/temple.js
// Wrapper for the Temple (Blasted Wastes Town).

import data from './BlastedWastesTown/Temple/temple.js';

const temple = {
  id: data.id,
  name: data.name,
  town: data.town,
  type: data.type,
  description: data.description,
  events: data.events,
  idols: data.idols,
};

export default temple;
