// src/data/townLocations/scavengerDoc.js
// Wrapper for the Scavenger Doc (Blasted Wastes Town).

import data from './BlastedWastesTown/ScavengerDoc/scavengerDoc.js';

const scavengerDoc = {
  id: data.id,
  name: data.name,
  town: data.town,
  type: data.type,
  description: data.description,
  events: data.events,
};

export default scavengerDoc;
