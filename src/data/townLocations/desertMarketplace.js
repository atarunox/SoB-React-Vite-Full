// src/data/townLocations/desertMarketplace.js
// Wrapper for the Desert Marketplace (Blasted Wastes Town).

import data from './BlastedWastesTown/DesertMarketplace/desertMarketplace.js';

const desertMarketplace = {
  id: data.id,
  name: data.name,
  town: data.town,
  type: data.type,
  description: data.description,
  events: data.events,
};

export default desertMarketplace;
