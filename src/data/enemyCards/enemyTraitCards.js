// src/data/enemyCards/enemyTraitCards.js
// Enemy-specific trait card decks.
// When an enemy with a trait deck appears, roll D6 — on 1-3 draw a random card from its deck.
// Format: { [enemyName]: [{ name, flavorText, effect }] }
// Populated by scanning via DM Options → Scan Cards → Enemy Trait Card, then merging the export.

export const ENEMY_TRAIT_CARDS = {};

export default ENEMY_TRAIT_CARDS;
